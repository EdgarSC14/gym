import type { APIRoute } from "astro"; import { db,row } from "@/lib/db";
export const POST:APIRoute=async({request,locals,redirect})=>{if(!locals.user)return redirect("/login");const form=await request.formData();const productId=Number(form.get("productId"));const rating=Number(form.get("rating"));const comment=String(form.get("comment")||"").trim();if(rating<1||rating>5||!comment||!await row("SELECT id_producto FROM producto WHERE id_producto=? AND esta_activo=1",[productId]))return redirect(`/products/${productId}`);await db.execute({sql:"INSERT INTO reseña_producto (id_producto,id_usuario,calificacion,comentario) VALUES (?,?,?,?)",args:[productId,locals.user.id,rating,comment]});await db.execute({sql:"UPDATE producto SET calificacion=(SELECT AVG(calificacion) FROM reseña_producto WHERE id_producto=?) WHERE id_producto=?",args:[productId,productId]});return redirect(`/products/${productId}`)};
